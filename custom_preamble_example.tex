% Document Class
\documentclass{amsart}

% Packages
\usepackage{amssymb,amsfonts}
\usepackage{tikz}
\usepackage{tikz-cd}

% User Defined Operators, Commands, and Macros
\DeclareMathOperator{\Z}{\mathbb{Z}}

% Remove page numbering, so page numbers do not render when imported into blender.
% Alternatively, use the standalone document class. However, sometimes this causes issues when using other packages. 
\pagestyle{empty}
\thispagestyle{empty}
